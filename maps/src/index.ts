import { User } from './User';
import { Company } from './Company';
import {} from 'googlemaps';

const user = new User();
console.log(user);

const company = new Company();
console.log(company);

google;
